package com.example.webpage.repository;

import com.example.webpage.model.AutomaticControlSystem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AutomaticControlSystemRepository extends JpaRepository<AutomaticControlSystem, Long> {
}